project:
  format: jarvis-hep-standalone-project
  version: 1
  output_root: outputs
  artifact_roots:
    outputs: outputs
    logs: logs
    images: images
    checkpoints: checkpoints
  path_markers:
    task_root: '&J'
  defaults:
    main_config: bin/GMFit_Validation_CSV.yaml
