# Examples

This folder is reserved for user-curated examples derived from `bin/` cards.

The bootstrap provides runnable cards under:

- `bin/quickstart_mcmc_operas.yaml`
- `bin/quickstart_csv_operas.yaml`
